var colors = require('colors'),
    LambdaDB = require('./lambdadb.js'),
    _ = require('underscore');

module.exports = function() {
    var LambdaDB = LambdaDB();
    console.log("SIGNUP:")

    console.log(colors.red('SIGN UP is not supported at the moment'));
}